{
  "Authentication": {
		"Jwt": {
			"Key": "%{JwtKey}%",
			"Issuer": "%{JwtIssuer}%",
			"Audiences": [ "%{FrontendUrl}%", "%{BackendUrl}%", "%{BackendUrlSecure}%" ],
			"RefreshTokenExpiration": 12 // 5 hours
		},
		"Google": {
			"ClientId": "%{GoogleClientId}%",
			"ClientSecret": "%{GoogleClientSecret}%",
			"RedirectUri": "%{GoogleRedirectUri}%"
		}
  }
}